Objectives:
- User writes the danish and the english.
- Generate by running /usr/local/bin/post; if web dir isn't writable, scream with appropriately helpful message (e.g. chown -R madda:www /var/www/htdocs/madda/pub)

Steps:
  - Check web dir is writable
  - Code locations in (~/.biblogual/posts (auto-create); openbsd httpd's location (check perms and complain if bad)

Later:
  RSS
  Handle images (one can be added to post, will appear at top)
  User management + friends-only section?
  Admin-only stats page?
